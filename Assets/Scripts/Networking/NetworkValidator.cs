using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public static class NetworkValidator
{
   /* public static bool isLocalPlayerTurn()
    {
        var localPlayer = GameHUD.Instance.LocalPlayer;
        return TurnManager.Instance.CurrentPlayer == localPlayer;
    }

    public static bool ValidateUnitOwnership(int unitId, Player player)
    {
        var unit = UnitManager.Instance.GetUnit(unitId);
        return unit != null && unit.Owner == player;
    }*/
}
